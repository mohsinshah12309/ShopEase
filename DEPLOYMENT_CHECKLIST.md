# Deployment Checklist — MERN E-commerce App (Backend → Render, Frontend → Vercel)

Generated by a pre-deployment review of the `server/` and `client/` folders.
No deploy commands were run; only the edits listed at the bottom were made.

---

## 1. Secrets & .gitignore Safety

- [ ] **CRITICAL — `server/.gitignore` DOES NOT EXIST** (and there is no root-level `.gitignore` either).
      This means `server/.env` is currently NOT protected and could be committed to git.
      👉 Create a `server/.gitignore` containing at least two lines: `node_modules` and `.env`.
- [x] `client/.gitignore` exists and lists `.env` (line 25) — `client/.env` is safe from being committed.
- [ ] **Rotate secrets if they were ever committed/pushed.** `server/.env` currently contains what look like
      LIVE credentials (MongoDB Atlas connection string with username/password, Stripe secret key,
      Cloudinary API secret, Gmail app password). If this folder has ever been pushed to any remote,
      rotate ALL of these before deploying.

## 2. Backend Environment Variables (paste into Render dashboard)

Every variable below is referenced somewhere in `server/` code via `process.env.X`.
⚠️ `server/.env.example` EXISTS but is **EMPTY**, so none of these were documented there. All 14 are listed here:

| #   | Variable                   | Used in                                                                       | Notes for production value                                                                                                                  |
| --- | -------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `PORT`                     | `server/server.js`                                                            | Render injects its own port — set to nothing, or keep; code falls back to 5000. Best practice: use `process.env.PORT` as-is (already does). |
| 2   | `MONGO_URI`                | `server/config/db.js`                                                         | Your MongoDB Atlas connection string (allow Render's IPs or 0.0.0.0/0 in Atlas Network Access).                                             |
| 3   | `JWT_SECRET`               | `server/controllers/authController.js`, `server/middleware/authMiddleware.js` | Long random string. Use a NEW strong value for production.                                                                                  |
| 4   | `STRIPE_SECRET_KEY`        | `server/controllers/orderController.js`                                       | Use your **live** (`sk_live_...`) or test secret key.                                                                                       |
| 5   | `CLOUDINARY_CLOUD_NAME`    | `server/config/cloudinary.js`, `server/middleware/upload.js`, test scripts    | From Cloudinary dashboard.                                                                                                                  |
| 6   | `CLOUDINARY_API_KEY`       | `server/config/cloudinary.js`, test scripts                                   | From Cloudinary dashboard.                                                                                                                  |
| 7   | `CLOUDINARY_API_SECRET`    | `server/config/cloudinary.js`, test scripts                                   | From Cloudinary dashboard.                                                                                                                  |
| 8   | `CLOUDINARY_UPLOAD_PRESET` | `server/middleware/upload.js`                                                 | Has code fallback `"ecommerce_unsigned"`.                                                                                                   |
| 9   | `CLOUDINARY_FOLDER`        | `server/middleware/upload.js`                                                 | Has code fallback `"ecommerce"`.                                                                                                            |
| 10  | `EMAIL_HOST`               | `server/utils/email.js`                                                       | e.g. `smtp.gmail.com`.                                                                                                                      |
| 11  | `EMAIL_PORT`               | `server/utils/email.js`                                                       | e.g. `587`.                                                                                                                                 |
| 12  | `EMAIL_USER`               | `server/utils/email.js`                                                       | Sender email address.                                                                                                                       |
| 13  | `EMAIL_PASS`               | `server/utils/email.js`                                                       | Gmail app password (not account password).                                                                                                  |
| 14  | `CLIENT_URL`               | `server/server.js` (CORS origin)                                              | **Must be your Vercel frontend URL**, e.g. `https://your-app.vercel.app`.                                                                   |

- [ ] Paste all 14 variables into Render → your Web Service → Environment.
- [ ] (Optional but recommended) Fill in `server/.env.example` with placeholder values matching the table above.

## 3. Frontend Environment Variables (paste into Vercel dashboard)

Code references found (only these two exist):

| Variable                      | Used in                         |
| ----------------------------- | ------------------------------- |
| `VITE_API_URL`                | `client/src/api/axios.js`       |
| `VITE_STRIPE_PUBLISHABLE_KEY` | `client/src/pages/Checkout.jsx` |

- [x] `client/.env.example` did not exist — **created during this review** with placeholders for both variables.
- [ ] Set `VITE_API_URL=https://<your-render-service>.onrender.com/api` in Vercel → Project Settings → Environment Variables.
- [ ] Set `VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...` (or pk_test) in Vercel.
- ⚠️ Note: Vite bakes `VITE_*` variables into the bundle **at build time** — after changing them you must trigger a redeploy.

## 4. Package Scripts

- [x] `server/package.json` has `"start": "node server.js"` (line 8) — ✅ Render requirement satisfied. No edit needed.
- [x] `client/package.json` has `"build": "vite build"` (line 8) and `"preview": "vite preview"` (line 10).
- [x] Build output goes to `dist/`: `client/vite.config.js` has no `outDir` override, so Vite's default `dist` applies. In Vercel set Output Directory = `dist`.

## 5. CORS Configuration

- [x] `server/server.js` line 30 uses `origin: process.env.CLIENT_URL` — ✅ correctly environment-driven, NOT hardcoded.
- [ ] Make sure the Render env var `CLIENT_URL` exactly matches your Vercel URL (including `https://`, no trailing slash), otherwise browsers will block API calls.

## 6. Hardcoded Localhost URLs

Searched all of `client/src` and all `server/` source code (config, controllers, middleware, models, routes, utils, server.js).

- [x] **No hardcoded `http://localhost:5000` or `http://localhost:5173` strings exist anywhere in application code.** Nothing to fix manually in source files.
- The only localhost references are inside the local `.env` files themselves (expected — these get replaced by dashboard values):
  - `server/.env` line 14: `CLIENT_URL=http://localhost:5173`
  - `client/.env` line 1: `VITE_API_URL=http://localhost:5000/api`
- [ ] When filling in the Render/Vercel dashboards, use the production URLs shown in sections 2–3 instead of these localhost values.

## 7. Suggested Platform Settings

**Render (backend):**

- Root Directory: `server`
- Build Command: `npm install`
- Start Command: `npm start`

**Vercel (frontend):**

- Root Directory: `client`
- Framework Preset: Vite
- Build Command: `npm run build`
- Output Directory: `dist`

---

## Edits Made During This Review

1. ❌→✅ Checked `server/package.json` for a `start` script — it already had one; **no edit made**.
2. ✅ Created `client/.env.example` (did not exist) listing `VITE_API_URL` and `VITE_STRIPE_PUBLISHABLE_KEY` with placeholder values.
3. ✅ Created this file, `DEPLOYMENT_CHECKLIST.md`.

No other files were modified.
